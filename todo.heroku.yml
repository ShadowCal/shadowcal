setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: development
release:
  command:
    - bundle exec rake db:migrate
run:
  web: bundle exec foreman start
  